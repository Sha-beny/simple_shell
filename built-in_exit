#include "shell.h"

/**
 * shell_exit - exit the shell
 * @args: Arguments
 *
 * return: 0
 */

void shell_exit(char **args)
{
	int stat = 0;

	if (args[1] != NULL)
	{
		stat = _atoi(args[1]);
	}
	free_token(args);
	free_last_input();
	exit(stat);
}
